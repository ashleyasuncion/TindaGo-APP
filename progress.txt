================================================================================
TINDAGO — KOTLIN ANDROID APP PROGRESS
================================================================================
Date: Aug 18–22, 2026
Project: TindaGo Android App (formerly Sari-sari_smart)
================================================================================

PROJECT OVERVIEW
--------------------------------------------------------------------------------
A native Android sari-sari store management app built with Kotlin and Jetpack
Compose. Ported from the HTML/CSS/JS prototype (TindaGo web app).

Originally built as "Sari-Sari Smart" (July–Aug 2026). Rebranded to "TindaGo"
on Aug 18, 2026.

All 5 frontend stages + Room Database + Notifications + Testing:
  Stage 1 — Project Foundation ✓
  Stage 2 — 16 Screens with full UI ✓
  Stage 3 — Tutorial System (12 page-specific tutorials) ✓
  Stage 4 — Room Database (9 entities, 9 DAOs, AppDatabase, AppRepository) ✓
  Stage 5 — UX Polish (snackbar, confirmations, nav badges) ✓
  Stage 6 — Local Notifications (WorkManager, 4 channels, rules + throttle) ✓
  Tests  — 89 unit tests (all passing) ✓

Three pillars: Stock Tracker | Daily Cash Summary | Utang Notebook | SMS Notifications

================================================================================
APP ARCHITECTURE (Current)
================================================================================

Tech Stack:
  - Language: Kotlin 2.0.21
  - UI: Jetpack Compose + Material3
  - Navigation: Navigation Compose
  -  Persistence: Room Database (11 entities, 11 DAOs, AppDatabase, AppRepository)
  - Settings: DataStore (tindago_prefs)
  - Notifications: WorkManager + NotificationCompat (4 channels)
  - DI: Manual (TindaGoApp.applicationContext)
  - State: StateFlow in AppViewModel, auto-saved via Room state flows
  - Testing: 89 unit tests (ViewModel + notification rules + throttle)

Package Structure:
  com.example.tindago/
    ├── data/
    │   ├── Models.kt              — Product, DailyEntry, SpecificSale, etc.
    │   ├── AppRepository.kt       — Wraps all 9 Room DAOs
    │   ├── SnackbarManager.kt     — CompositionLocal for global snackbar
    │   ├── TimeUtils.kt           — Date/time helpers
    │   ├── local/
    │   │   ├── AppDatabase.kt     — Room database singleton (tindago_db)
    │   │   ├── Converters.kt      — Type converters
    │   │   ├── dao/               — 11 DAOs (Product, DailyEntry, SpecificSale,
    │   │   │                        CustomerDebt, EndOfDay, RestockLog,
    │   │   │                        DebtPayment, DebtTransaction, Expense,
    │   │   │                        SmsLog)
    │   │   └── entity/            — 11 Room entities
    │   ├── SmsHelper.kt          — PH phone validation + one-tap SMS intent
    │   └── notifications/
    │       ├── AppForegroundTracker.kt — Suppresses pushes while app is open
    │       ├── DailyCheckWorker.kt     — WorkManager worker, twice daily
    │       ├── SmsWorker.kt            — Auto SMS reminders via SmsManager
    │       ├── SmsScheduler.kt         — Schedules/cancels SMS worker
    │       ├── NotificationCenter.kt   — Notification posting + deep links
    │       ├── NotificationChannels.kt — 4 channel definitions
    │       ├── NotificationRules.kt    — Pure rule logic
    │       └── NotificationThrottle.kt — Cooldown/pinning logic
    ├── ui/
    │   ├── components/            — TutorialOverlay, TutorialHighlightState,
    │   │                            DeveloperPanel, NotificationPrimerDialog,
    │   │                            AppHeader
    │   ├── localization/          — Strings.kt (EN/FIL), AppSettings.kt
    │   ├── navigation/            — NavGraph.kt, BottomNavBar.kt, Routes.kt
    │   ├── screens/               — 20 screens (3-moment: Morning/Day/Closing
    │   │                            + 17 support screens)
    │   └── theme/                 — Color.kt, Theme.kt, Type.kt
    ├── MainActivity.kt
    └── TindaGoApp.kt

================================================================================
SCREENS (20 — all fully implemented)
================================================================================

  Three Moments:
  [✓] MorningCheckScreen    — Morning dashboard (stock/debt cards, start day)
  [✓] DayModeScreen         — Live stats, transaction feed, FAB sale sheet
  [✓] EveningClosingScreen  — Expenses/earnings, sold items, complete overlay

  Support Screens:
  [✓] SplashScreen            — Animated splash with auto-navigate
  [✓] SetupScreen             — First-time setup (store name, owner, language)
  [✓] StocksScreen            — Inventory with visual status indicators
  [✓] AddStockScreen          — Add/edit product with markup pricing
  [✓] ProductDetailScreen     — Product detail with stock adjustment
  [✓] DebtsScreen             — Customer debt list
  [✓] NewDebtScreen           — Add new debt entry
  [✓] CustomerDebtDetailScreen — Individual customer ledger
  [✓] RecordPaymentScreen     — Record debt payment
  [✓] CheckoutScreen          — Cart checkout
  [✓] ExpensesScreen          — Store expenses log
  [✓] RestockScreen           — Restock day management
  [✓] ReportsScreen           — Day/Week/Month reports with KPIs
  [✓] SettingsScreen          — Language, text size, store info, notifications
  [✓] HelpScreen              — Help menu, replay tutorial, about/contact

================================================================================
V1.0 — REBRAND: SARISARISMART → TINDAGO (MOBILE) — DONE
Date: Aug 18, 2026
Status: IMPLEMENTED + VERIFIED

WHAT CHANGED (user-facing):
- App launcher name: "TindaGo" (was "Sari-sari_smart")
- Splash screen text: "TindaGo" (was "Sari-Sari Smart")
- All user-facing Strings.kt entries updated (welcome, tutorial1, aboutContent,
  helpTutorial1, notifTestBody) in both EN and FIL
- Support email: support@tindago.com (was support@sarisarismart.com)

WHAT CHANGED (code):
- Package: com.example.sari_sari_smart → com.example.tindago
  (48 main + 3 test + 9 androidTest files moved, package decls + imports updated,
   435 references across 60 files)
- Class: SariSariApp → TindaGoApp (file + manifest + 4 referencing files)
- Theme: SariSariSmartTheme → TindaGoTheme (30 files);
  style Theme.Sarisari_smart → Theme.TindaGo (themes.xml ×2)
- Deep links: scheme sarisarismart:// → tindago://
  (manifest + 4 NotificationCenter URI constants)
- Room DB: sari_sari_smart_db → tindago_db
- DataStore prefs: sss_prefs → tindago_prefs
- Notification icon: ic_stat_sari.xml → ic_stat_tindago.xml
- CSV headers: "TindaGo — Data Export" / "TindaGo Report"
- Export filenames: tindago-data.json, tindago-data.csv,
  tindago-report-YYYY-MM-DD.csv

WHAT CHANGED (config):
- app/build.gradle.kts: namespace + applicationId → com.example.tindago
- settings.gradle.kts: rootProject.name → TindaGo
- AndroidManifest.xml: .TindaGoApp, Theme.TindaGo, tindago:// deep links

DATA/STORAGE:
- Room database renamed from sari_sari_smart_db to tindago_db — old data will
  NOT carry over (applicationId change means fresh install)
- DataStore prefs renamed from sss_prefs to tindago_prefs

DEPENDENCIES/INFRASTRUCTURE:
- Git remote still points at old GitHub repo (Sari-sari_smart_APP.git) — pending
  GitHub rename
- support@tindago.com domain must exist before shipping
- androidTest set has pre-existing compile errors in CustomerDebtDaoTest.kt
  (lastActivity String-vs-Long drift) — unrelated to rebrand

VERIFICATION:
- ./gradlew compileDebugKotlin — BUILD SUCCESSFUL.
- ./gradlew testDebugUnitTest — BUILD SUCCESSFUL; 89/89 tests pass under
  com.example.tindago package.

================================================================================
V1.1 — CLEANUP: REMOVE DEV ARTIFACTS (MOBILE) — DONE
Date: Aug 18, 2026
Status: IMPLEMENTED — dev-only files removed, tutorials retained

REMOVED (no longer needed for the app to build/run):
- .idea/ (10 files — Android Studio IDE config)
- .kotlin/errors/ (5 files — stale compiler error logs)
- .kotlin/sessions/ (1 file — compiler session cache)
- progress.txt (old Sari-Sari Smart changelog)
- adaptation_plan.txt, adaptation_plan2.txt, adaptation_plan3.txt (planning docs)
- docs/architecture.md, docs/user_research.md (dev documentation)
- app/src/test/ (4 files — unit tests)
- app/src/androidTest/ (10 files — instrumented tests)

KEPT:
- tutorials/ (17 files — tutorial spec docs for the overlay system)

FILES REMAINING (source only):
  Main source (48 .kt files): MainActivity, TindaGoApp, Models, AppRepository,
    SnackbarManager, TimeUtils, AppDatabase, Converters, 9 DAOs, 9 entities,
    6 notification files, AppSettings, Strings, NavGraph, BottomNavBar, Routes,
    20 screens, 5 components, 3 theme files
  Config (8): .gitignore ×2, build.gradle.kts ×2, settings.gradle.kts,
    gradle.properties, gradlew, gradlew.bat
  Resources: AndroidManifest.xml, rules.keep, drawables, mipmaps, values, xml
  Build tooling: gradle/ (wrapper + libs.versions.toml)
  Tutorials (17): README + 16 tutorial spec files

================================================================================
V2.0 — SAMPLE DATA OVERHAUL + UI POLISH (MOBILE) — DONE
Date: Aug 18, 2026
Status: IMPLEMENTED + VERIFIED

V2.0a — 120-item sample dataset
─────────────────────────────────────────────────────────────────────────────
WHAT CHANGED (user-facing):
- Default inventory expanded from 17 products → 120 products
- 20 product categories (was 9): Soft Drinks, Bottled Water, Instant Coffee,
  Instant Noodles, Rice, Canned Sardines, Canned Tuna, Eggs, Bread/Pandesal,
  Biscuits/Cookies, Chocolate/Candy, Chips/Snacks, Salt, Sugar, Shampoo
  Sachets, Bath Soap, Laundry Detergent, Toothpaste/Toothbrush, Mosquito
  Coils, Cigarettes
- 5 new unit types (was 12): sack, loaf, tube, bar, sticks
- Every category has 4-6 competing brands
- Stock variety: 13 out-of-stock, 17 low-stock, 90 plenty
- Old dataset auto-migrated: if ≤17 products found in Room DB, replaced
  with new 120-item dataset

WHAT CHANGED (code):
- Models.kt: ProductCatalog.CATEGORIES expanded 9 → 20
- Models.kt: ProductCatalog.UNITS expanded 12 → 17 (+sack, loaf, tube, bar, sticks)
- AppViewModel.kt: seedSampleData() replaced with 120-item dataset
- AppViewModel.kt: initRepository() — migration detection (size <= 17)
- Strings.kt: 20 new cat* i18n keys (EN + FIL)
- Strings.kt: 5 new unit* i18n keys (EN + FIL)
- Strings.kt: productCategoryLabel() updated when-map for 20 categories
- Strings.kt: productUnitLabel() updated when-map for 17 units

V2.0b — Category filter chips: horizontal scroll + expand
─────────────────────────────────────────────────────────────────────────────
WHAT CHANGED (user-facing):
- Category chip row changed from wrapping to horizontal scroll
- Shows first 6 chips (All + 5 categories) in a single row
- "More ▾" / "Dagdag ▾" button at the end to expand all 20
- "Less ▴" / "Bawas ▴" button to collapse back

WHAT CHANGED (code):
- StocksScreen.kt: added catExpanded state variable
- StocksScreen.kt: LazyRow shows visibleKeys (6 or all) + More/Less toggle
- FilterChip with green accent colors for More/Less button

V2.0c — Morning stock card: top 2 items only
─────────────────────────────────────────────────────────────────────────────
WHAT CHANGED (user-facing):
- Morning page stock warning card now shows only top 2 items
- Out-of-stock items shown first, then low-stock
- "+N more" / "+N pang" indicator for remaining items
- Card stays compact instead of growing with every low item

WHAT CHANGED (code):
- MorningCheckScreen.kt: sortedConcerned = outOfStock + lowStock (out first)
- shownItems = sortedConcerned.take(2)
- moreCount indicator with EN/FIL text

V2.0d — Stocks page back-to-top button
─────────────────────────────────────────────────────────────────────────────
WHAT CHANGED (user-facing):
- Floating circle button appears at bottom-right of stocks page
- Button appears when user scrolls past the category filter chips
- Tap to smooth-scroll back to top of stocks list
- Button disappears when at top

WHAT CHANGED (code):
- StocksScreen.kt: wrapped content in Box for FAB overlay
- StocksScreen.kt: added LazyListState + rememberCoroutineScope
- StocksScreen.kt: derivedStateOf — showBackToTop when firstVisibleItem > 2
- StocksScreen.kt: FloatingActionButton with upward arrow

FILES AFFECTED:
  data/Models.kt (ProductCatalog categories + units)
  ui/screens/AppViewModel.kt (seedSampleData, migration)
  ui/screens/StocksScreen.kt (cat filter expand, back-to-top FAB)
  ui/screens/MorningCheckScreen.kt (top 2 stock items)
  ui/localization/Strings.kt (i18n keys + label functions)

VERIFICATION:
- ./gradlew compileDebugKotlin — BUILD SUCCESSFUL
- ./gradlew testDebugUnitTest — BUILD SUCCESSFUL

================================================================================
V2.0e — BUG FIX: CATEGORY CHIPS EXPAND NOW WRAPS (MOBILE) — DONE
Date: Aug 19, 2026
Status: IMPLEMENTED + VERIFIED

PROBLEM:
- V2.0b used LazyRow for both collapsed and expanded states
- When expanded, all 21 chips were stuffed into one LazyRow, making the
  horizontal scroller longer instead of wrapping to multiple lines
- Web version correctly uses flex-wrap:wrap when expanded

SOLUTION:
- Switched from single LazyRow to conditional layout:
    Collapsed: LazyRow (horizontal scroll, 6 chips + "More ▾")
    Expanded:  FlowRow (chips wrap to multiple lines + "Less ▴")
- Added @OptIn(ExperimentalLayoutApi::class) for FlowRow

WHAT CHANGED (code):
- StocksScreen.kt: added import FlowRow (from foundation.layout)
- StocksScreen.kt: wrapped chip layout in if/else — LazyRow when collapsed,
  FlowRow when expanded
- StocksScreen.kt: added @OptIn(ExperimentalLayoutApi::class) annotation

FILES AFFECTED:
  ui/screens/StocksScreen.kt

VERIFICATION:
- ./gradlew compileDebugKotlin — BUILD SUCCESSFUL
- ./gradlew testDebugUnitTest — BUILD SUCCESSFUL

================================================================================
V2.0f — BUG FIX: INVENTORY BOTTOM GAP (MOBILE) — DONE
Date: Aug 19, 2026
Status: IMPLEMENTED + VERIFIED

PROBLEM:
- Web app has 80px bottom padding on .app-content, creating a gap between
  the last inventory item and the bottom nav bar
- Mobile app had 0px bottom gap — last item sat flush against the bottom nav

SOLUTION:
- Added contentPadding parameter to StocksScreen (accepts Scaffold's
  PaddingValues)
- Applied 80.dp bottom padding to LazyColumn, matching web's 80px
- Passed paddingValues from NavGraph.kt → StocksScreen

WHAT CHANGED (code):
- StocksScreen.kt: added contentPadding: PaddingValues = PaddingValues(0.dp)
  parameter; LazyColumn contentPadding now adds 80.dp bottom
- NavGraph.kt: passed paddingValues from SupportScaffold content lambda to
  StocksScreen contentPadding parameter

FILES AFFECTED:
  ui/screens/StocksScreen.kt
  ui/navigation/NavGraph.kt

VERIFICATION:
- ./gradlew compileDebugKotlin — BUILD SUCCESSFUL
- ./gradlew testDebugUnitTest — BUILD SUCCESSFUL

================================================================================
V2.0g — BUG FIX: REMOVE INVENTORY BOTTOM GAP + DOUBLE PADDING (MOBILE)
Date: Aug 19, 2026
Status: IMPLEMENTED

PROBLEM:
- StocksScreen had an 80.dp bottom padding (V2.0f) incorrectly matching
  the web app's broken padding-bottom:80px
- The bottom nav is a flex sibling in the web app but sits outside the
  content area in the mobile app (Scaffold handles it), so no extra
  padding was needed
- Additionally, SupportScaffold applies Modifier.padding(paddingValues)
  on a Box wrapping the content, AND passed paddingValues to
  StocksScreen's contentPadding — causing DOUBLE padding (top + bottom)

SOLUTION:
- Removed the 80.dp extra bottom padding from StocksScreen LazyColumn
- Removed the contentPadding parameter entirely from StocksScreen
  (unused — Scaffold's Box already handles all padding)
- Removed contentPadding = paddingValues from NavGraph.kt StocksScreen call

WHAT CHANGED (code):
- StocksScreen.kt: removed contentPadding parameter and contentPadding
  line from LazyColumn (padding now handled by SupportScaffold's Box)
- NavGraph.kt: removed contentPadding = paddingValues from StocksScreen call

FILES AFFECTED:
  ui/screens/StocksScreen.kt
  ui/navigation/NavGraph.kt

VERIFICATION:
- ./gradlew compileDebugKotlin — BUILD SUCCESSFUL
- ./gradlew testDebugUnitTest — BUILD SUCCESSFUL

================================================================================

V2.0h — TUTORIAL AUTO-SCROLL RESTORATION (MOBILE) — DONE
Date: Aug 20, 2026
Status: IMPLEMENTED + VERIFIED

PROBLEM:
- Tutorial auto-scroll-to-highlight was broken in the mobile app
- The code existed in TutorialOverlay.kt but could never execute
- Root cause: TutorialOverlay is composed OUTSIDE the scope where screens
  provide LocalScreenScrollState / LocalScreenLazyListState
- The overlay always read null for both scroll states
- 3 screens (StocksScreen, RecordPaymentScreen, NewDebtScreen) also lacked
  scroll state providers entirely

SOLUTION:
- Created TutorialScrollStateHolder (shared mutable state holder) that bridges
  scroll state between screen composables and the TutorialOverlay
- Screens update the holder via LaunchedEffect when they compose
- TutorialOverlay reads from the holder instead of CompositionLocals
- Holder provided at NavGraph level via CompositionLocalProvider
- Added missing scroll state providers to 3 screens

BUILD FIX #1 — Smart cast impossible on delegated properties:
- Using `by rememberUpdatedState(...)` creates Kotlin delegated properties
- Kotlin cannot smart-cast delegated properties (compiler can't prove
  value hasn't changed between null-check and usage)
- Fix: read scrollHolder.scrollState / scrollHolder.lazyListState into
  local vals inside the LaunchedEffect block before null-checking

BUILD FIX #2 — JVM signature clash on TutorialScrollStateHolder:
- `var scrollState by mutableStateOf(...)` auto-generates setScrollState()
  on JVM, which clashed with the explicit `fun setScrollState()`
- Same for lazyListState / setLazyListState
- Fix: changed to backing fields (_scrollState, _lazyListState) with public
  val getters; renamed setters to updateScrollState() / updateLazyListState()
- Updated all 16 screen files that call the renamed methods

COVERAGE — tutorials with steps requiring auto-scroll:
  Main tutorial: steps 3 (startDayBtn), 9 (completeDayBtn)
  Sales/Day: step 7 (dayTxFeed)
  EOD/Closing: step 5 (completeDayBtn)
  Report: steps 4-5 (reportBestSellers, reportLowStock)
  Add Product: step 4 (addStockSaveBtn)
  Checkout: steps 3-4 (checkoutPayCredit, checkoutComplete)
  New Debt: step 3 (newDebtSaveBtn)
  Help: steps 3-4 (helpContact, helpAbout)
  Restock: steps 4, 6-7 (restockContinueBtn, restockPurchaseList, restockDoneBtn)
  Product Detail: step 3 (pdDeleteBtn)
  Customer Debt Detail: step 3 (cddRecordPaymentBtn)
  Record Payment: steps 2-3 (rpNoteField, rpPayBtn)
  Expenses: step 2 (expenseList)
  (16 of 18 tutorials have at least one step where scroll may be needed)

FILES AFFECTED:
  ui/components/TutorialScrollStateHolder.kt (NEW)
  ui/components/TutorialOverlay.kt
  ui/navigation/NavGraph.kt
  ui/screens/MorningCheckScreen.kt, DayModeScreen.kt, EveningClosingScreen.kt
  ui/screens/AddStockScreen.kt, CheckoutScreen.kt, HelpScreen.kt
  ui/screens/ExpensesScreen.kt, CustomerDebtDetailScreen.kt
  ui/screens/ReportsScreen.kt, SettingsScreen.kt, ProductDetailScreen.kt
  ui/screens/DebtsScreen.kt, RestockScreen.kt
  ui/screens/StocksScreen.kt, RecordPaymentScreen.kt, NewDebtScreen.kt

VERIFICATION:
- ./gradlew assembleDebug — BUILD SUCCESSFUL
- ./gradlew testDebugUnitTest — BUILD SUCCESSFUL

================================================================================
V2.0i — RESTOCK STICKY BARS + BACK-TO-TOP + TUTORIAL FIX (MOBILE) — DONE
Date: Aug 20, 2026
Status: IMPLEMENTED + VERIFIED

V2.0i-a — Sticky bottom action bars (both steps)
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- On Step 1, the "Continue to Purchases →" button was at the bottom of the
  LazyColumn product list, requiring users to scroll all the way down.
- On Step 2, the "Done ✓" button had the same issue.

SOLUTION:
- Both steps now have a sticky bottom action bar using Box +
  AnimatedVisibility + Alignment.BottomCenter.
- Content has bottom padding (80.dp) so last items clear the bar.

Step 1 sticky bar: [Cancel] [Continue to Purchases →]
Step 2 sticky bar: [← Back] [Done ✓]

WHAT CHANGED (code):
- RestockScreen.kt: restructured from Column-only to Box layout for
  overlay positioning.
- Added AnimatedVisibility for Step 1 bar (visible when restockTemp.step == 1)
  with Cancel (OutlinedButton) + Continue (Button with tutorialHighlight).
- Added AnimatedVisibility for Step 2 bar (visible when restockTemp.step == 2)
  with Back (OutlinedButton) + Done (Button with tutorialHighlight).
- LazyColumn contentPadding increased to 80.dp for sticky bar clearance.
- Back button calls viewModel.setRestockStep(1).
- Continue calls viewModel.applyCorrectionsToProducts() + setRestockStep(2).
- Done calls viewModel.completeRestock() + onComplete().

FILES AFFECTED:
  ui/screens/RestockScreen.kt

V2.0i-b — Back-to-top FAB (Step 1 only)
─────────────────────────────────────────────────────────────────────────────
WHAT CHANGED (user-facing):
- Floating circle button appears at bottom-right of restock Step 1
- Appears when firstVisibleItemIndex > 0 on the step 1 LazyColumn
- Tap scrolls back to item 0
- Hidden on Step 2

WHAT CHANGED (code):
- RestockScreen.kt: added showBackToTop derivedStateOf (step == 1 &&
  firstVisibleItemIndex > 0)
- AnimatedVisibility + FloatingActionButton with upward arrow, positioned
  at Alignment.BottomEnd with 80.dp bottom padding (above sticky bar)
- onClick: scope.launch { step1ListState.animateScrollToItem(0) }

FILES AFFECTED:
  ui/screens/RestockScreen.kt

V2.0i-c — Tutorial step transition fix
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- The restock page tutorial has 8 steps, all pinned to the "restock" page.
  When advancing from step 5 (restockContinueBtn) to step 6
  (restockStep2Section), the screen still shows Step 1 — Step 2's UI
  elements don't exist, so the tutorial highlight frame has nothing to
  attach to.

SOLUTION:
- Added restock step-transition triggers in NavGraph.kt:
  - advanceTutorial(): when pageTutorial?.id == "restock" and next >= 5,
    call appViewModel.setRestockStep(2) so Step 2 renders before the
    overlay tries to highlight restockStep2Section.
  - previousTutorial(): when pageTutorial?.id == "restock" and prev <= 4,
    call appViewModel.setRestockStep(1) so Step 1 renders before the
    overlay tries to highlight restockStep1Section.

WHAT CHANGED (code):
- NavGraph.kt: added restock step-transition check in advanceTutorial()
  after incrementing tutorialStep.
- NavGraph.kt: added restock step-transition check in previousTutorial()
  after decrementing tutorialStep.

FILES AFFECTED:
  ui/navigation/NavGraph.kt

V2.0i-d — Restock tutorial text updated
─────────────────────────────────────────────────────────────────────────────
WHAT CHANGED (user-facing):
- restockTutorial6: Added "Use the Back button to return to Step 1 if needed."
- restockTutorial8: Changed "Find the reminder on Morning page." →
  "You can also go Back to make changes before finishing."

WHAT CHANGED (code):
- Strings.kt: updated EN i18n strings for restockTutorial6 and
  restockTutorial8

FILES AFFECTED:
  ui/localization/Strings.kt

ALL FILES AFFECTED (V2.0i):
  ui/screens/RestockScreen.kt
  ui/navigation/NavGraph.kt
  ui/localization/Strings.kt

VERIFICATION:
- ./gradlew compileDebugKotlin — BUILD SUCCESSFUL

================================================================================
V2.0j — CHECKOUT CART LINE COLORS + i18n FIXES (MOBILE) — DONE
Date: Aug 20, 2026
Status: IMPLEMENTED + VERIFIED

V2.0j-a — eachLabel FIL translation fix
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- Mobile eachLabel FIL was "bawat" (missing "isa").
- Web eachLabel FIL is "bawat isa" (correct).
- The label appears next to the selling price in cart lines (e.g. "₱5.00").

SOLUTION:
- Changed FIL string from "bawat" to "bawat isa" to match web.

WHAT CHANGED (code):
- Strings.kt: eachLabel FIL: "bawat" → "bawat isa"

V2.0j-b — Settings "More" section FIL translation fix
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- Settings page "More" section title was translated as "Higit Pa".
- "Higit" means "greater than / exceeding" — unnatural as a section heading.

SOLUTION:
- Changed FIL translation from "Higit Pa" to "Iba Pa" ("Others" /
  "Additional"). Standard Filipino UI pattern for catch-all grouping.

WHAT CHANGED (code):
- Strings.kt: moreSection FIL: "Higit Pa" → "Iba Pa"

V2.0j-c — Checkout CartLine colors synced to web
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- CartLine colors diverged from web CSS values:
    Sub-line: Green700 (mobile) vs --text-muted #94a3b8 (web)
    Price: Gray400 (mobile) vs --text-secondary #64748b (web)
    Subtotal: Gray800 (mobile) vs --primary #16a34a (web)

SOLUTION:
- Updated CartLine composable colors to match web CSS variables:
    Sub-line: Green700 → Gray400 (#94A3B8 = --text-muted)
    Price: Gray400 → Gray500 (#64748B = --text-secondary)
    Subtotal: Gray800 → Green600 (#16A34A = --primary)

WHAT CHANGED (code):
- CheckoutScreen.kt: CartLine subline color Green700 → Gray400
- CheckoutScreen.kt: CartLine price color Gray400 → Gray500
- CheckoutScreen.kt: CartLine subtotal color Gray800 → Green600

ALL FILES AFFECTED (V2.0j):
  ui/localization/Strings.kt
  ui/screens/CheckoutScreen.kt

VERIFICATION:
- ./gradlew compileDebugKotlin — BUILD SUCCESSFUL

================================================================================
V2.0k — CLOSING REMINDER HOUR DEVICE-AWARE TIME FORMAT (MOBILE) — DONE
Date: Aug 20, 2026
Status: IMPLEMENTED + VERIFIED

PROBLEM:
- Closing Reminder Hour setting displayed time in fixed 24-hour format
  (e.g. "18:00") regardless of the user's device time format preference.
- Changing the device from 24-hour to 12-hour format had no effect on the
  app's time display.
- The raw integer field with ":00" suffix was never designed for locale-aware
  formatting.

SOLUTION:
- Added android.text.format.DateFormat.is24HourFormat(context) detection
  to adapt the display/input to the device's preferred time format.

  24-hour mode: [ 18 ] :00  (unchanged behavior)
  12-hour mode: [ 6 ] :00 [PM]  (12-hour value + AM/PM toggle button)

- The stored closingReminderHour Int (6-21) and DailyCheckWorker
  notification logic remain unchanged — only the display/input format
  adapts.

WHAT CHANGED (code):
- SettingsScreen.kt: added import android.text.format.DateFormat
- SettingsScreen.kt: closing reminder hour section now branches on
  is24HourFormat(context):
    24h: existing OutlinedTextField with :00 suffix
    12h: OutlinedTextField (1-12) + FilledTonalButton AM/PM toggle;
         hour conversion between 12h display and 24h storage on both
         input and toggle tap

FILES AFFECTED:
  ui/screens/SettingsScreen.kt

VERIFICATION:
- ./gradlew compileDebugKotlin — BUILD SUCCESSFUL

================================================================================
V2.0l — SETTINGS PAGE REORGANIZATION (MOBILE) — DONE
Date: Aug 20, 2026
Status: IMPLEMENTED + VERIFIED

PROBLEM:
- Settings page was a flat, ungrouped list of10 sections with no logical
  hierarchy. Each inventory default (Markup, Threshold, Credit Limit) had
  its own section title + spacer. Language was separate from Store Info.
  Tutorials and More (Reports/Help) were navigation shortcuts mixed in
  with actual settings. Dev test buttons (Test Notification, Run Check Now)
  were visible to end users. No Reset Data option.

SOLUTION:
- Reorganized into 6 logical groups:
    Store Profile: Store Name + Owner Name + Language
    Display: Text Size
    Inventory Defaults: Markup + Threshold + Credit Limit (one group)
    Notifications: Master toggle + toggles + reminder hour (dev buttons removed)
    Support: Tutorials selector + Help + Reports
    Data: Export + Import + Reset (red, destructive)
- Removed dev test buttons (Test Notification, Run Check Now) from
  Notifications section — these belong in Developer Panel only.
- Added Reset All Data button (red, calls viewModel.resetAllData())
- All tutorial highlight target IDs preserved (settingsLanguage,
  settingsTextSize, settingsStoreName, settingsOwnerName,
  settingsDefaultMarkup, settingsLowStockThreshold,
  settingsDefaultCreditLimit)

WHAT CHANGED (code):
- SettingsScreen.kt: restructured Column content into6 grouped sections;
  merged Language into Store Profile; merged 3 inventory defaults into
  one section; removed dev test buttons; added Reset All Data button
  (Red600 background); renamed More to Support (Tutorials + Help + Reports)
- Strings.kt: added6 new i18n keys (EN + FIL): settingsSectionProfile,
  settingsSectionDisplay, settingsSectionDefaults, settingsSectionSupport,
  settingsSectionData, resetDataBtn

FILES AFFECTED:
  ui/screens/SettingsScreen.kt
  ui/localization/Strings.kt

VERIFICATION:
- ./gradlew compileDebugKotlin — BUILD SUCCESSFUL

================================================================================
V2.0m — CLOSING PAGE COLLAPSIBLE SECTIONS + LOW STOCK FILTER FIX (MOBILE) — DONE
Date: Aug 20, 2026
Status: IMPLEMENTED + VERIFIED

V2.0m-a — Collapsible sold items, low stock, debts sections
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- Closing page sections (Mga Nabenta, Mga Kulang sa Stock, Utang Hindi Pa
  Bayad) were always expanded, requiring scrolling through all content.

SOLUTION:
- Made all3 sections collapsible with AnimatedVisibility.
- Section title is clickable Row with up/down arrow icon.
- All3 default to expanded (expanded = true).

WHAT CHANGED (code):
- EveningClosingScreen.kt: added3 collapse state variables
  (soldItemsExpanded, lowStockExpanded, debtsExpanded)
- Each section title changed from static Text to clickable Row
  with Icons.Default.KeyboardArrowUp/Down
- Each section content wrapped in AnimatedVisibility(visible = expanded)
- Added imports: AnimatedVisibility, clickable, Icons,
  KeyboardArrowUp, KeyboardArrowDown

V2.0m-b — Low stock filter: prioritize low stock over out-of-stock
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- Filter was products.filter { it.status != StockStatus.PLENTY }
  sorted by quantity. With take(5), all5 slots were filled by
  out-of-stock items (qty=0 sorts first), hiding low stock items.
- 13 out-of-stock items in seed data meant low stock items were
  completely invisible in the closing page.

SOLUTION:
- Split into two separate filters:
    LOW items first (sorted by quantity ascending)
    OUT_OF_STOCK items next (sorted by name)
  Concatenated and take(5).
- Low stock items now appear first, giving shopkeepers actionable
  alerts before the out-of-stock items.

WHAT CHANGED (code):
- EveningClosingScreen.kt: replaced single filter with two filters
  (LOW + OUT_OF_STOCK), concatenated as displayItems = (lowItems +
  outItems).take(5)

FILES AFFECTED (V2.0m):
  ui/screens/EveningClosingScreen.kt

VERIFICATION:
- ./gradlew compileDebugKotlin — BUILD SUCCESSFUL

================================================================================
V2.0n — RESTOCK DATE FIX + TUTORIAL REPLAY HINT (MOBILE) — DONE
Date: Aug 20, 2026
Status: IMPLEMENTED + VERIFIED

V2.0n-a — Corrupted restock date auto-clear
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- daysSinceLastRestock getter had no date validation. Corrupted or
  invalid dates in Room restockLog could produce unexpected values.

SOLUTION:
- Added 4 guards in daysSinceLastRestock:
    Date is null → returns -1 (unchanged)
    Parse fails → clears _lastRestockDate, returns -1
    Days < 0 → clears _lastRestockDate, returns -1
    Days > 365 → clears _lastRestockDate, returns -1

WHAT CHANGED (code):
- AppViewModel.kt: daysSinceLastRestock — added try/catch around
  SimpleDateFormat.parse(), added days < 0 and days > 365 guards
  with _lastRestockDate.value = null fallback

FILES AFFECTED:
  ui/screens/AppViewModel.kt

V2.0n-b — Tutorial replay hint (? button highlight)
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- The header ? button (TutorialIconButton) was never highlighted in any
  tutorial. Users had no in-tutorial cue that they could replay tutorials.

SOLUTION:
- Added replayHintKey parameter to PageTutorial data class.
- When set, a final step is appended to the tutorial that highlights the
  header ? button with text: "You can always tap the ? button to replay
  this tutorial."
- All 19 PageTutorial definitions updated with replayHintKey.

WHAT CHANGED (code):
- TutorialOverlay.kt: added replayHintKey: String? = null to PageTutorial;
  added ReplayHintStep data class; added appendReplayHintStep() function;
  added .replayHint() modifier extension for highlight target
- NavGraph.kt: getCurrentTutorialSteps() now appends replay hint step
  when tutorial.replayHintKey is set
- Strings.kt: added tutorialReplayHint i18n key (EN + FIL)

FILES AFFECTED (V2.0n):
  ui/components/TutorialOverlay.kt
  ui/navigation/NavGraph.kt
  ui/localization/Strings.kt

VERIFICATION:
- ./gradlew compileDebugKotlin — BUILD SUCCESSFUL

================================================================================
V2.0o — BFCACHE LANGUAGE FIX + LANGUAGE CHIP BUTTONS (MOBILE) — IN PROGRESS
Date: Aug 22, 2026
Status: IN PROGRESS

NOTE: Mobile app is NOT affected by bfcache stale data. Compose reactive
state via LocalLanguage CompositionLocal propagates instantly. Language
chip buttons already match the desired UX (Material3 FilterChips).
Mobile work below is for parity with web V3.0 changes only.

V2.0o-a — Language change propagation investigation
─────────────────────────────────────────────────────────────────────────────
INVESTIGATION FINDING:
- Mobile app uses LocalLanguage CompositionLocal (mutableStateOf)
- Changing language in SettingsScreen directly mutates langState.value
- This triggers automatic recomposition of ALL screens — no navigation
  or page reload needed
- No bfcache equivalent exists in Compose Navigation
- VERDICT: Mobile is NOT affected by the web's bfcache language bug

V2.0o-b — Language change UX (already matches mobile design)
─────────────────────────────────────────────────────────────────────────────
INVESTIGATION FINDING:
- Mobile already uses FilterChip-based language selector (two pill buttons)
- Web is being upgraded to match this pattern (web V3.0)
- Mobile needs NO changes for this — already has the better UX

NO CODE CHANGES REQUIRED FOR MOBILE IN THIS VERSION.
Mobile progress updated for documentation/parity tracking purposes.

================================================================================
V2.1a — SMS DEBT NOTIFICATIONS: PHASE 1 — PHONE + ONE-TAP SMS (MOBILE) — DONE
Date: Aug 24, 2026
Status: IMPLEMENTED + VERIFIED

PROBLEM:
- Store owners have no way to send debt receipts or reminders to customers
- Customer debtor records only store a name — no phone number
- No SMS capability exists in the app

SOLUTION:
- Added phone number field to the CustomerDebt data model
- Added SmsHelper utility for PH phone validation + one-tap SMS intent
- Added Send Receipt / Send Reminder buttons to CustomerDebtDetail screen
- Added phone input to New Debt form and Checkout (credit) flow
- Added SMS section to Settings (informational)
- Full EN/FIL i18n support

DATA MODEL CHANGES:
- Models.kt: added phoneNumber: String and smsOptIn: Boolean? to CustomerDebt
- CustomerDebtEntity.kt: added phoneNumber and smsOptIn columns + mapping
- AppDatabase.kt: Room migration v9→v10 (non-destructive column add)
- AppRepository.kt: added updateDebtPhoneNumber(), updateDebtSmsOptIn()
- AppViewModel.kt: added updateDebtPhoneNumber(), updateDebtSmsOptIn(),
  getStoreName()

NEW FILES:
- SmsHelper.kt — PH phone validation (09XX, +63), one-tap SMS intent via
  ACTION_SENDTO, message builders (receipt, reminder, paid confirmation)

UI CHANGES:
- NewDebtScreen: phone input field (optional, with PH format hint)
- CheckoutScreen: phone field appears when Credit payment is selected;
  phone saved to debt record on credit checkout
- CustomerDebtDetailScreen: phone card (show/edit) + Send Receipt / Send
  Reminder buttons that open the phone's messaging app with pre-composed text
- SettingsScreen: SMS Notifications info section

WHAT CHANGED (code):
- Models.kt: CustomerDebt data class — +phoneNumber, +smsOptIn
- CustomerDebtEntity.kt: +phoneNumber, +smsOptIn columns, entityToModel()
- AppDatabase.kt: version 9→10, MIGRATION_9_10, +smsLogDao
- AppRepository.kt: +updateDebtPhoneNumber(), +updateDebtSmsOptIn()
- AppViewModel.kt: +updateDebtPhoneNumber(), +updateDebtSmsOptIn(),
  +getStoreName()
- SmsHelper.kt (NEW): validatePhoneNumber(), normalizePhoneNumber(),
  buildReceiptMessage(), buildReminderMessage(), buildPaidMessage(),
  sendSmsIntent()
- Strings.kt: 18 EN/FIL SMS i18n keys
- NewDebtScreen.kt: phone OutlinedTextField + validation
- CheckoutScreen.kt: phone field (credit mode) + save to debt
- CustomerDebtDetailScreen.kt: phone card + SMS action buttons
- SettingsScreen.kt: SMS info section

FILES AFFECTED:
  data/Models.kt
  data/SmsHelper.kt (NEW)
  data/local/entity/CustomerDebtEntity.kt
  data/local/AppDatabase.kt
  data/AppRepository.kt
  ui/screens/AppViewModel.kt
  ui/screens/NewDebtScreen.kt
  ui/screens/CheckoutScreen.kt
  ui/screens/CustomerDebtDetailScreen.kt
  ui/screens/SettingsScreen.kt
  ui/localization/Strings.kt

VERIFICATION:
- ./gradlew compileDebugKotlin — BUILD SUCCESSFUL
- ./gradlew testDebugUnitTest — BUILD SUCCESSFUL
- ./gradlew assembleDebug — BUILD SUCCESSFUL (APK generated)

================================================================================
V2.1b — SMS DEBT NOTIFICATIONS: PHASE 2 — AUTOMATED SMS VIA SmsManager (MOBILE)
Date: Aug 24, 2026
Status: IMPLEMENTED + VERIFIED

PROBLEM:
- Phase 1 required the owner to manually tap Send in the messaging app
- Owners want automatic, scheduled debt reminders without manual effort
- The app already has WorkManager infrastructure (DailyCheckWorker)

SOLUTION:
- Added SmsWorker using WorkManager to send SMS reminders automatically
- Added SmsLogEntity to track every send attempt (success/failure)
- Added SMS settings: master toggle, frequency (3/7/14 days), quiet hours
- Added SEND_SMS permission to AndroidManifest.xml
- SmsWorker respects quiet hours, per-debtor opt-out, and deduplication
- Wired SmsScheduler into TindaGoApp.kt (schedules on app launch)

NEW FILES:
- SmsWorker.kt — WorkManager worker that sends SMS reminders automatically
- SmsLogEntity.kt — Room entity for SMS delivery tracking
- SmsLogDao.kt — DAO for SMS log queries

DATA MODEL CHANGES:
- AppDatabase.kt: version 10→11, MIGRATION_10_11, +smsLogDao, +SmsLogEntity
- AppSettings.kt: +smsEnabled, +smsReminderDays, +smsQuietHoursStart,
  +smsQuietHoursEnd, +smsSendPaidConfirmation

PERMISSION:
- AndroidManifest.xml: added SEND_SMS permission
- SettingsScreen: runtime permission request when user enables SMS toggle

UI CHANGES:
- SettingsScreen: SMS Notifications section with master toggle, frequency
  selector (3/7/14 days), quiet hours inputs, paid confirmation toggle
- Permission request flow on first enable

WHAT CHANGED (code):
- SmsWorker.kt (NEW): doWork() — checks permission, filters active debts
  with phone numbers, checks quiet hours, deduplicates via throttle,
  groups multi-debt per customer into ONE message, sends via
  SmsManager.sendTextMessage(), logs result to sms_log table
- SmsLogEntity.kt (NEW): id, debtId, customerName, phoneNumber, message,
  timestamp, success, errorMessage
- SmsLogDao.kt (NEW): insert, getByDebtId, getRecent, deleteOlderThan
- SmsScheduler.kt (NEW): scheduleSmsReminders() / cancelSmsReminders()
- AppDatabase.kt: version 10→11, MIGRATION_10_11
- AppRepository.kt: +smsLogDao, +insertSmsLog(), +deleteOldSmsLogs()
- AppSettings.kt: 5 new SMS preferences
- TindaGoApp.kt: schedules SmsWorker on app launch via SmsScheduler
- SettingsScreen.kt: SMS section with working controls + permission request
- Strings.kt: SMS settings i18n keys (EN + FIL)
- AndroidManifest.xml: SEND_SMS permission

HOW IT WORKS:
  App Launch → TindaGoApp.scheduleSmsReminders()
      ↓ reads settings (enabled? frequency?)
      ↓ WorkManager schedules periodic SmsWorker
  SmsWorker fires every N days
      ↓ checks SEND_SMS permission
      ↓ filters active debts with phone numbers
      ↓ checks quiet hours (default 8:00–20:00)
      ↓ checks last reminder timestamp (avoids duplicates)
      ↓ groups multiple debts per customer into ONE message
      ↓ sends via SmsManager (owner's SIM)
      ↓ logs result to sms_log table

CAVEAT:
- SmsManager requires SEND_SMS permission → Google Play blocks this for
  non-default-SMS-handler apps. Works for sideloaded/beta APKs only
  (fits capstone demo, not Play Store release).

FILES AFFECTED:
  data/notifications/SmsWorker.kt (NEW)
  data/notifications/SmsScheduler.kt (NEW)
  data/local/entity/SmsLogEntity.kt (NEW)
  data/local/dao/SmsLogDao.kt (NEW)
  data/local/AppDatabase.kt
  data/AppRepository.kt
  ui/localization/AppSettings.kt
  ui/screens/SettingsScreen.kt
  ui/localization/Strings.kt
  TindaGoApp.kt
  AndroidManifest.xml

VERIFICATION:
- ./gradlew compileDebugKotlin — BUILD SUCCESSFUL
- ./gradlew testDebugUnitTest — BUILD SUCCESSFUL
- ./gradlew assembleDebug — BUILD SUCCESSFUL (APK generated)

================================================================================
V2.1c — HIGH PRIORITY FIXES: NOTIFICATION TIMING + AUTO-RECEIPT (MOBILE) — DONE
Date: Aug 24, 2026
Status: IMPLEMENTED + VERIFIED

V2.1c-a — Notification timing ambiguity fix
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- Settings UI showed "Closing reminder hour" alongside toggles for other
  notification types (store left open, out of stock, restock)
- Users couldn't tell if 18:00 was a global delivery time or closing-only
- Quiet hours (6:00–21:00) were hardcoded and invisible in Settings

SOLUTION:
- Renamed label to "Closing reminder time"
- Added description: "When the closing reminder fires (overdue, stock &
  restock alerts arrive separately between 6 AM–9 PM)."
- Added "Notification timing" info card explaining morning vs closing
  notification schedules

V2.1c-b — Auto-receipt SMS prompt after credit sale
─────────────────────────────────────────────────────────────────────────────
PROBLEM:
- After completing a credit sale in CheckoutScreen, the owner had to
  manually navigate to CustomerDebtDetail and tap "Send Receipt"
- This extra step was easy to forget

SOLUTION:
- After completing a credit sale with a phone number, an AlertDialog
  pops up: "Send SMS Receipt?"
- Send button → opens messaging app with pre-composed receipt
- Skip button → dismisses the dialog

WHAT CHANGED (code):
- Strings.kt: renamed closingReminderHour → closingReminderTime, added
  closingReminderDesc, notifTimingTitle, notifTimingDesc, smsReceiptDialogTitle,
  smsReceiptDialogMsg, smsReceiptSend, smsReceiptSkip (EN + FIL)
- SettingsScreen.kt: updated closing reminder label, added description,
  added notification timing info card
- CheckoutScreen.kt: added showSmsReceiptDialog state, AlertDialog with
  Send/Skip buttons, SmsHelper.sendSmsIntent() on Send

FILES AFFECTED:
  ui/localization/Strings.kt
  ui/screens/SettingsScreen.kt
  ui/screens/CheckoutScreen.kt

VERIFICATION:
- ./gradlew compileDebugKotlin — BUILD SUCCESSFUL
- ./gradlew testDebugUnitTest — BUILD SUCCESSFUL

================================================================================
